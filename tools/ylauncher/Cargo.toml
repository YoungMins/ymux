[package]
name = "ylauncher"
version = "0.1.0"
edition = "2021"
description = "The `y` binary: relays Claude Code hook events into ymux's agent tree"

[[bin]]
name = "y"
path = "src/main.rs"

[dependencies]
yipc = { path = "../../crates/yipc" }
serde_json = "1"
